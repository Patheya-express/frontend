/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { AvailablePartnerCurrentAssignmentDto } from '../models/available-partner-current-assignment-dto';
import { AvailablePartnerLocationDto } from '../models/available-partner-location-dto';
export interface AvailableDeliveryPartnerResponseDto {

  /**
   * The partner's current non-terminal order, if any — modeled as an array for forward compatibility, though this domain only ever has at most one.
   */
  currentAssignments: Array<AvailablePartnerCurrentAssignmentDto>;
  location: AvailablePartnerLocationDto;
  name: string;

  /**
   * Live Redis presence — distinct from `status`, the durable DB flag.
   */
  online: boolean;

  /**
   * The DeliveryPartner's own id — pass this as deliveryPartnerId to POST /admin/orders/:orderId/assign.
   */
  partnerId: string;
  phone: string;
  status: 'OFFLINE' | 'AVAILABLE' | 'ON_DELIVERY' | 'SUSPENDED';
  vehicle: 'BIKE' | 'SCOOTER' | 'BICYCLE' | 'CAR' | 'ELECTRIC_VEHICLE';
}
