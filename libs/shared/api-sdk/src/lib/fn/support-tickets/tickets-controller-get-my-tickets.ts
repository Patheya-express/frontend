/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { PaginatedTicketsResponseDto } from '../../models/paginated-tickets-response-dto';

export interface TicketsControllerGetMyTickets$Params {
  category?: any;
  status?: any;
  limit?: number;
  page?: number;
}

export function ticketsControllerGetMyTickets(http: HttpClient, rootUrl: string, params?: TicketsControllerGetMyTickets$Params, context?: HttpContext): Observable<StrictHttpResponse<PaginatedTicketsResponseDto>> {
  const rb = new RequestBuilder(rootUrl, ticketsControllerGetMyTickets.PATH, 'get');
  if (params) {
    rb.query('category', params.category, {});
    rb.query('status', params.status, {});
    rb.query('limit', params.limit, {});
    rb.query('page', params.page, {});
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<PaginatedTicketsResponseDto>;
    })
  );
}

ticketsControllerGetMyTickets.PATH = '/api/v1/tickets/me';
