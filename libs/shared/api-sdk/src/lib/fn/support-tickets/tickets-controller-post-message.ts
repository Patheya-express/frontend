/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { CreateTicketMessageDto } from '../../models/create-ticket-message-dto';
import { TicketResponseDto } from '../../models/ticket-response-dto';

export interface TicketsControllerPostMessage$Params {
  id: string;
      body: CreateTicketMessageDto
}

export function ticketsControllerPostMessage(http: HttpClient, rootUrl: string, params: TicketsControllerPostMessage$Params, context?: HttpContext): Observable<StrictHttpResponse<TicketResponseDto>> {
  const rb = new RequestBuilder(rootUrl, ticketsControllerPostMessage.PATH, 'post');
  if (params) {
    rb.path('id', params.id, {});
    rb.body(params.body, 'application/json');
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<TicketResponseDto>;
    })
  );
}

ticketsControllerPostMessage.PATH = '/api/v1/tickets/{id}/messages';
