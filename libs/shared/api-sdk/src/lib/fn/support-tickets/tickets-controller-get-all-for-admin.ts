/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { PaginatedAdminTicketsResponseDto } from '../../models/paginated-admin-tickets-response-dto';

export interface TicketsControllerGetAllForAdmin$Params {
  unassigned?: boolean;
  assignedAgentId?: any;
  priority?: any;
  category?: any;
  status?: any;
  search?: any;
  limit?: number;
  page?: number;
}

export function ticketsControllerGetAllForAdmin(http: HttpClient, rootUrl: string, params?: TicketsControllerGetAllForAdmin$Params, context?: HttpContext): Observable<StrictHttpResponse<PaginatedAdminTicketsResponseDto>> {
  const rb = new RequestBuilder(rootUrl, ticketsControllerGetAllForAdmin.PATH, 'get');
  if (params) {
    rb.query('unassigned', params.unassigned, {});
    rb.query('assignedAgentId', params.assignedAgentId, {});
    rb.query('priority', params.priority, {});
    rb.query('category', params.category, {});
    rb.query('status', params.status, {});
    rb.query('search', params.search, {});
    rb.query('limit', params.limit, {});
    rb.query('page', params.page, {});
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<PaginatedAdminTicketsResponseDto>;
    })
  );
}

ticketsControllerGetAllForAdmin.PATH = '/api/v1/tickets/admin';
