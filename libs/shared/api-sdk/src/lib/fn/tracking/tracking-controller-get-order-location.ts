/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { OrderLocationResponseDto } from '../../models/order-location-response-dto';

export interface TrackingControllerGetOrderLocation$Params {

/**
 * Order ID
 */
  orderId: string;
}

export function trackingControllerGetOrderLocation(http: HttpClient, rootUrl: string, params: TrackingControllerGetOrderLocation$Params, context?: HttpContext): Observable<StrictHttpResponse<OrderLocationResponseDto>> {
  const rb = new RequestBuilder(rootUrl, trackingControllerGetOrderLocation.PATH, 'get');
  if (params) {
    rb.path('orderId', params.orderId, {});
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<OrderLocationResponseDto>;
    })
  );
}

trackingControllerGetOrderLocation.PATH = '/api/v1/tracking/order/{orderId}';
