/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { OrderLocationResponseDto } from '../../models/order-location-response-dto';
import { UpdateLocationDto } from '../../models/update-location-dto';

export interface TrackingControllerUpdateLocation$Params {
      body: UpdateLocationDto
}

export function trackingControllerUpdateLocation(http: HttpClient, rootUrl: string, params: TrackingControllerUpdateLocation$Params, context?: HttpContext): Observable<StrictHttpResponse<OrderLocationResponseDto>> {
  const rb = new RequestBuilder(rootUrl, trackingControllerUpdateLocation.PATH, 'post');
  if (params) {
    rb.body(params.body, 'application/json');
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<OrderLocationResponseDto>;
    })
  );
}

trackingControllerUpdateLocation.PATH = '/api/v1/tracking/location';
