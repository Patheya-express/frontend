/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';


export interface BranchesControllerRemove$Params {
  restaurantId: string;
  branchId: string;
}

export function branchesControllerRemove(http: HttpClient, rootUrl: string, params: BranchesControllerRemove$Params, context?: HttpContext): Observable<StrictHttpResponse<void>> {
  const rb = new RequestBuilder(rootUrl, branchesControllerRemove.PATH, 'delete');
  if (params) {
    rb.path('restaurantId', params.restaurantId, {});
    rb.path('branchId', params.branchId, {});
  }

  return http.request(
    rb.build({ responseType: 'text', accept: '*/*', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return (r as HttpResponse<any>).clone({ body: undefined }) as StrictHttpResponse<void>;
    })
  );
}

branchesControllerRemove.PATH = '/api/v1/restaurants/{restaurantId}/branches/{branchId}';
