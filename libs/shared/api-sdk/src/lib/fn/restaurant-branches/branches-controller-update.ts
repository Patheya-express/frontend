/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { BranchResponseDto } from '../../models/branch-response-dto';
import { UpdateBranchDto } from '../../models/update-branch-dto';

export interface BranchesControllerUpdate$Params {
  restaurantId: string;
  branchId: string;
      body: UpdateBranchDto
}

export function branchesControllerUpdate(http: HttpClient, rootUrl: string, params: BranchesControllerUpdate$Params, context?: HttpContext): Observable<StrictHttpResponse<BranchResponseDto>> {
  const rb = new RequestBuilder(rootUrl, branchesControllerUpdate.PATH, 'patch');
  if (params) {
    rb.path('restaurantId', params.restaurantId, {});
    rb.path('branchId', params.branchId, {});
    rb.body(params.body, 'application/json');
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<BranchResponseDto>;
    })
  );
}

branchesControllerUpdate.PATH = '/api/v1/restaurants/{restaurantId}/branches/{branchId}';
