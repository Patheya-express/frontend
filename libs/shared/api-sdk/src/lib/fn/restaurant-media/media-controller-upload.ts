/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { MediaResponseDto } from '../../models/media-response-dto';

export interface MediaControllerUpload$Params {
  restaurantId: string;
      body: {
'file'?: Blob;
'type'?: string;
'branchId'?: string;
}
}

export function mediaControllerUpload(http: HttpClient, rootUrl: string, params: MediaControllerUpload$Params, context?: HttpContext): Observable<StrictHttpResponse<MediaResponseDto>> {
  const rb = new RequestBuilder(rootUrl, mediaControllerUpload.PATH, 'post');
  if (params) {
    rb.path('restaurantId', params.restaurantId, {});
    rb.body(params.body, 'multipart/form-data');
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<MediaResponseDto>;
    })
  );
}

mediaControllerUpload.PATH = '/api/v1/restaurants/{restaurantId}/media';
