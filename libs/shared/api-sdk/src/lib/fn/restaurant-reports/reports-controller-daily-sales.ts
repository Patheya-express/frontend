/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { ReportResponseDto } from '../../models/report-response-dto';

export interface ReportsControllerDailySales$Params {
  restaurantId: string;

/**
 * Inclusive start date (ISO). Defaults per report type.
 */
  dateFrom?: string;

/**
 * Inclusive end date (ISO). Defaults per report type.
 */
  dateTo?: string;

/**
 * Restrict to a single branch. Omit for all branches.
 */
  branchId?: string;

/**
 * Orders Report only.
 */
  status?: 'PENDING' | 'CONFIRMED' | 'PREPARING' | 'READY_FOR_PICKUP' | 'OUT_FOR_DELIVERY' | 'DELIVERED' | 'CANCELLED';

/**
 * Payment Summary only.
 */
  paymentMode?: 'ONLINE' | 'COD';

/**
 * json (default) returns a preview payload; csv/xlsx/pdf return a file download.
 */
  format?: 'json' | 'csv' | 'xlsx' | 'pdf';
}

export function reportsControllerDailySales(http: HttpClient, rootUrl: string, params: ReportsControllerDailySales$Params, context?: HttpContext): Observable<StrictHttpResponse<ReportResponseDto>> {
  const rb = new RequestBuilder(rootUrl, reportsControllerDailySales.PATH, 'get');
  if (params) {
    rb.path('restaurantId', params.restaurantId, {});
    rb.query('dateFrom', params.dateFrom, {});
    rb.query('dateTo', params.dateTo, {});
    rb.query('branchId', params.branchId, {});
    rb.query('status', params.status, {});
    rb.query('paymentMode', params.paymentMode, {});
    rb.query('format', params.format, {});
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<ReportResponseDto>;
    })
  );
}

reportsControllerDailySales.PATH = '/api/v1/reports/{restaurantId}/daily-sales';
