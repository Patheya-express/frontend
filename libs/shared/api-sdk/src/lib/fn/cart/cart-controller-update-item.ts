/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { CartResponseDto } from '../../models/cart-response-dto';
import { UpdateCartItemDto } from '../../models/update-cart-item-dto';

export interface CartControllerUpdateItem$Params {
  itemId: string;
      body: UpdateCartItemDto
}

export function cartControllerUpdateItem(http: HttpClient, rootUrl: string, params: CartControllerUpdateItem$Params, context?: HttpContext): Observable<StrictHttpResponse<CartResponseDto>> {
  const rb = new RequestBuilder(rootUrl, cartControllerUpdateItem.PATH, 'patch');
  if (params) {
    rb.path('itemId', params.itemId, {});
    rb.body(params.body, 'application/json');
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<CartResponseDto>;
    })
  );
}

cartControllerUpdateItem.PATH = '/api/v1/cart/items/{itemId}';
