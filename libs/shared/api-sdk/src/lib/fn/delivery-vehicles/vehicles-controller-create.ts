/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { CreateVehicleDto } from '../../models/create-vehicle-dto';
import { VehicleResponseDto } from '../../models/vehicle-response-dto';

export interface VehiclesControllerCreate$Params {
      body: CreateVehicleDto
}

export function vehiclesControllerCreate(http: HttpClient, rootUrl: string, params: VehiclesControllerCreate$Params, context?: HttpContext): Observable<StrictHttpResponse<VehicleResponseDto>> {
  const rb = new RequestBuilder(rootUrl, vehiclesControllerCreate.PATH, 'post');
  if (params) {
    rb.body(params.body, 'application/json');
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<VehicleResponseDto>;
    })
  );
}

vehiclesControllerCreate.PATH = '/api/v1/delivery/vehicles';
