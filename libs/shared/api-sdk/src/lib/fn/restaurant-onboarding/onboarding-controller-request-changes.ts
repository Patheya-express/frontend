/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { OnboardingResponseDto } from '../../models/onboarding-response-dto';
import { RequestOnboardingChangesDto } from '../../models/request-onboarding-changes-dto';

export interface OnboardingControllerRequestChanges$Params {
  restaurantId: string;
      body: RequestOnboardingChangesDto
}

export function onboardingControllerRequestChanges(http: HttpClient, rootUrl: string, params: OnboardingControllerRequestChanges$Params, context?: HttpContext): Observable<StrictHttpResponse<OnboardingResponseDto>> {
  const rb = new RequestBuilder(rootUrl, onboardingControllerRequestChanges.PATH, 'patch');
  if (params) {
    rb.path('restaurantId', params.restaurantId, {});
    rb.body(params.body, 'application/json');
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<OnboardingResponseDto>;
    })
  );
}

onboardingControllerRequestChanges.PATH = '/api/v1/restaurants/{restaurantId}/onboarding/request-changes';
