/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { DocumentVersionResponseDto } from '../../models/document-version-response-dto';

export interface AdminDocumentsControllerHistory$Params {
  deliveryPartnerId: string;
  documentType: 'DRIVING_LICENSE' | 'AADHAAR' | 'PAN' | 'VEHICLE_RC' | 'VEHICLE_INSURANCE' | 'VEHICLE_FITNESS' | 'VEHICLE_POLLUTION' | 'PROFILE_PHOTO' | 'SELFIE' | 'BACKGROUND_VERIFICATION' | 'VEHICLE_PHOTO' | 'OTHER';
}

export function adminDocumentsControllerHistory(http: HttpClient, rootUrl: string, params: AdminDocumentsControllerHistory$Params, context?: HttpContext): Observable<StrictHttpResponse<Array<DocumentVersionResponseDto>>> {
  const rb = new RequestBuilder(rootUrl, adminDocumentsControllerHistory.PATH, 'get');
  if (params) {
    rb.path('deliveryPartnerId', params.deliveryPartnerId, {});
    rb.path('documentType', params.documentType, {});
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<Array<DocumentVersionResponseDto>>;
    })
  );
}

adminDocumentsControllerHistory.PATH = '/api/v1/delivery/admin/{deliveryPartnerId}/documents/{documentType}/history';
