/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { DeliveryDocumentResponseDto } from '../../models/delivery-document-response-dto';
import { RejectDeliveryDocumentDto } from '../../models/reject-delivery-document-dto';

export interface AdminDocumentsControllerReject$Params {
  deliveryPartnerId: string;
  documentId: string;
      body: RejectDeliveryDocumentDto
}

export function adminDocumentsControllerReject(http: HttpClient, rootUrl: string, params: AdminDocumentsControllerReject$Params, context?: HttpContext): Observable<StrictHttpResponse<DeliveryDocumentResponseDto>> {
  const rb = new RequestBuilder(rootUrl, adminDocumentsControllerReject.PATH, 'patch');
  if (params) {
    rb.path('deliveryPartnerId', params.deliveryPartnerId, {});
    rb.path('documentId', params.documentId, {});
    rb.body(params.body, 'application/json');
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<DeliveryDocumentResponseDto>;
    })
  );
}

adminDocumentsControllerReject.PATH = '/api/v1/delivery/admin/{deliveryPartnerId}/documents/{documentId}/reject';
