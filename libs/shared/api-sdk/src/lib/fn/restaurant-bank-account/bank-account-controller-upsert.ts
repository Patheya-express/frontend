/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { BankAccountResponseDto } from '../../models/bank-account-response-dto';
import { UpsertBankAccountDto } from '../../models/upsert-bank-account-dto';

export interface BankAccountControllerUpsert$Params {
  restaurantId: string;
      body: UpsertBankAccountDto
}

export function bankAccountControllerUpsert(http: HttpClient, rootUrl: string, params: BankAccountControllerUpsert$Params, context?: HttpContext): Observable<StrictHttpResponse<BankAccountResponseDto>> {
  const rb = new RequestBuilder(rootUrl, bankAccountControllerUpsert.PATH, 'patch');
  if (params) {
    rb.path('restaurantId', params.restaurantId, {});
    rb.body(params.body, 'application/json');
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<BankAccountResponseDto>;
    })
  );
}

bankAccountControllerUpsert.PATH = '/api/v1/restaurants/{restaurantId}/bank-account';
