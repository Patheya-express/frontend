/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { RestaurantSettingsResponseDto } from '../../models/restaurant-settings-response-dto';
import { UpsertRestaurantSettingsDto } from '../../models/upsert-restaurant-settings-dto';

export interface SettingsControllerUpsert$Params {
  restaurantId: string;
      body: UpsertRestaurantSettingsDto
}

export function settingsControllerUpsert(http: HttpClient, rootUrl: string, params: SettingsControllerUpsert$Params, context?: HttpContext): Observable<StrictHttpResponse<RestaurantSettingsResponseDto>> {
  const rb = new RequestBuilder(rootUrl, settingsControllerUpsert.PATH, 'patch');
  if (params) {
    rb.path('restaurantId', params.restaurantId, {});
    rb.body(params.body, 'application/json');
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<RestaurantSettingsResponseDto>;
    })
  );
}

settingsControllerUpsert.PATH = '/api/v1/restaurants/{restaurantId}/settings';
