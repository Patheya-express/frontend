/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { RestaurantSettingsResponseDto } from '../../models/restaurant-settings-response-dto';

export interface SettingsControllerFind$Params {
  restaurantId: string;
}

export function settingsControllerFind(http: HttpClient, rootUrl: string, params: SettingsControllerFind$Params, context?: HttpContext): Observable<StrictHttpResponse<RestaurantSettingsResponseDto>> {
  const rb = new RequestBuilder(rootUrl, settingsControllerFind.PATH, 'get');
  if (params) {
    rb.path('restaurantId', params.restaurantId, {});
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<RestaurantSettingsResponseDto>;
    })
  );
}

settingsControllerFind.PATH = '/api/v1/restaurants/{restaurantId}/settings';
