/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { PushTokenResponseDto } from '../../models/push-token-response-dto';
import { RegisterPushTokenDto } from '../../models/register-push-token-dto';

export interface NotificationsControllerRegisterPushToken$Params {
      body: RegisterPushTokenDto
}

export function notificationsControllerRegisterPushToken(http: HttpClient, rootUrl: string, params: NotificationsControllerRegisterPushToken$Params, context?: HttpContext): Observable<StrictHttpResponse<PushTokenResponseDto>> {
  const rb = new RequestBuilder(rootUrl, notificationsControllerRegisterPushToken.PATH, 'post');
  if (params) {
    rb.body(params.body, 'application/json');
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<PushTokenResponseDto>;
    })
  );
}

notificationsControllerRegisterPushToken.PATH = '/api/v1/notifications/push-token';
