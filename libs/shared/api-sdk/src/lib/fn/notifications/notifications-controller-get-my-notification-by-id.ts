/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { NotificationResponseDto } from '../../models/notification-response-dto';

export interface NotificationsControllerGetMyNotificationById$Params {

/**
 * Notification ID
 */
  id: string;
}

export function notificationsControllerGetMyNotificationById(http: HttpClient, rootUrl: string, params: NotificationsControllerGetMyNotificationById$Params, context?: HttpContext): Observable<StrictHttpResponse<NotificationResponseDto>> {
  const rb = new RequestBuilder(rootUrl, notificationsControllerGetMyNotificationById.PATH, 'get');
  if (params) {
    rb.path('id', params.id, {});
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<NotificationResponseDto>;
    })
  );
}

notificationsControllerGetMyNotificationById.PATH = '/api/v1/notifications/me/{id}';
