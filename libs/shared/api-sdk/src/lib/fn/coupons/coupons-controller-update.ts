/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { CouponResponseDto } from '../../models/coupon-response-dto';
import { UpdateCouponDto } from '../../models/update-coupon-dto';

export interface CouponsControllerUpdate$Params {
  id: string;
      body: UpdateCouponDto
}

export function couponsControllerUpdate(http: HttpClient, rootUrl: string, params: CouponsControllerUpdate$Params, context?: HttpContext): Observable<StrictHttpResponse<CouponResponseDto>> {
  const rb = new RequestBuilder(rootUrl, couponsControllerUpdate.PATH, 'patch');
  if (params) {
    rb.path('id', params.id, {});
    rb.body(params.body, 'application/json');
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<CouponResponseDto>;
    })
  );
}

couponsControllerUpdate.PATH = '/api/v1/coupons/{id}';
