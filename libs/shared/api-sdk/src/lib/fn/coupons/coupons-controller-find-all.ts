/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { PaginatedCouponsResponseDto } from '../../models/paginated-coupons-response-dto';

export interface CouponsControllerFindAll$Params {
  active?: boolean;
  search?: any;
  limit?: number;
  page?: number;
}

export function couponsControllerFindAll(http: HttpClient, rootUrl: string, params?: CouponsControllerFindAll$Params, context?: HttpContext): Observable<StrictHttpResponse<PaginatedCouponsResponseDto>> {
  const rb = new RequestBuilder(rootUrl, couponsControllerFindAll.PATH, 'get');
  if (params) {
    rb.query('active', params.active, {});
    rb.query('search', params.search, {});
    rb.query('limit', params.limit, {});
    rb.query('page', params.page, {});
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<PaginatedCouponsResponseDto>;
    })
  );
}

couponsControllerFindAll.PATH = '/api/v1/coupons';
