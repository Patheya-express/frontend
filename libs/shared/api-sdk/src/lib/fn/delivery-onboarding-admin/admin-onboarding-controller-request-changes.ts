/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { DeliveryOnboardingResponseDto } from '../../models/delivery-onboarding-response-dto';
import { RequestDeliveryOnboardingChangesDto } from '../../models/request-delivery-onboarding-changes-dto';

export interface AdminOnboardingControllerRequestChanges$Params {
  deliveryPartnerId: string;
      body: RequestDeliveryOnboardingChangesDto
}

export function adminOnboardingControllerRequestChanges(http: HttpClient, rootUrl: string, params: AdminOnboardingControllerRequestChanges$Params, context?: HttpContext): Observable<StrictHttpResponse<DeliveryOnboardingResponseDto>> {
  const rb = new RequestBuilder(rootUrl, adminOnboardingControllerRequestChanges.PATH, 'patch');
  if (params) {
    rb.path('deliveryPartnerId', params.deliveryPartnerId, {});
    rb.body(params.body, 'application/json');
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<DeliveryOnboardingResponseDto>;
    })
  );
}

adminOnboardingControllerRequestChanges.PATH = '/api/v1/delivery/admin/{deliveryPartnerId}/onboarding/request-changes';
