/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { DispatchDebugInfoResponseDto } from '../../models/dispatch-debug-info-response-dto';

export interface AdminDispatchControllerGetDispatchDebugInfo$Params {
  orderId: string;
}

export function adminDispatchControllerGetDispatchDebugInfo(http: HttpClient, rootUrl: string, params: AdminDispatchControllerGetDispatchDebugInfo$Params, context?: HttpContext): Observable<StrictHttpResponse<DispatchDebugInfoResponseDto>> {
  const rb = new RequestBuilder(rootUrl, adminDispatchControllerGetDispatchDebugInfo.PATH, 'get');
  if (params) {
    rb.path('orderId', params.orderId, {});
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<DispatchDebugInfoResponseDto>;
    })
  );
}

adminDispatchControllerGetDispatchDebugInfo.PATH = '/api/v1/admin/orders/{orderId}/dispatch-debug';
