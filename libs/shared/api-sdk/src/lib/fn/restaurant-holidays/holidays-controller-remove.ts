/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';


export interface HolidaysControllerRemove$Params {
  restaurantId: string;
  holidayId: string;
}

export function holidaysControllerRemove(http: HttpClient, rootUrl: string, params: HolidaysControllerRemove$Params, context?: HttpContext): Observable<StrictHttpResponse<void>> {
  const rb = new RequestBuilder(rootUrl, holidaysControllerRemove.PATH, 'delete');
  if (params) {
    rb.path('restaurantId', params.restaurantId, {});
    rb.path('holidayId', params.holidayId, {});
  }

  return http.request(
    rb.build({ responseType: 'text', accept: '*/*', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return (r as HttpResponse<any>).clone({ body: undefined }) as StrictHttpResponse<void>;
    })
  );
}

holidaysControllerRemove.PATH = '/api/v1/restaurants/{restaurantId}/holidays/{holidayId}';
