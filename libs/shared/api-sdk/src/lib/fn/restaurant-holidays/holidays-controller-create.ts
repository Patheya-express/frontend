/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { CreateHolidayDto } from '../../models/create-holiday-dto';
import { HolidayResponseDto } from '../../models/holiday-response-dto';

export interface HolidaysControllerCreate$Params {
  restaurantId: string;
      body: CreateHolidayDto
}

export function holidaysControllerCreate(http: HttpClient, rootUrl: string, params: HolidaysControllerCreate$Params, context?: HttpContext): Observable<StrictHttpResponse<HolidayResponseDto>> {
  const rb = new RequestBuilder(rootUrl, holidaysControllerCreate.PATH, 'post');
  if (params) {
    rb.path('restaurantId', params.restaurantId, {});
    rb.body(params.body, 'application/json');
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<HolidayResponseDto>;
    })
  );
}

holidaysControllerCreate.PATH = '/api/v1/restaurants/{restaurantId}/holidays';
