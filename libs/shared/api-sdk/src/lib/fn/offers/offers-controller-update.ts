/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { OfferResponseDto } from '../../models/offer-response-dto';
import { UpdateOfferDto } from '../../models/update-offer-dto';

export interface OffersControllerUpdate$Params {
  id: string;
      body: UpdateOfferDto
}

export function offersControllerUpdate(http: HttpClient, rootUrl: string, params: OffersControllerUpdate$Params, context?: HttpContext): Observable<StrictHttpResponse<OfferResponseDto>> {
  const rb = new RequestBuilder(rootUrl, offersControllerUpdate.PATH, 'patch');
  if (params) {
    rb.path('id', params.id, {});
    rb.body(params.body, 'application/json');
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<OfferResponseDto>;
    })
  );
}

offersControllerUpdate.PATH = '/api/v1/offers/{id}';
