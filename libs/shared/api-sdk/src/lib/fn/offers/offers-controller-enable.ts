/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { OfferResponseDto } from '../../models/offer-response-dto';

export interface OffersControllerEnable$Params {
  id: string;
}

export function offersControllerEnable(http: HttpClient, rootUrl: string, params: OffersControllerEnable$Params, context?: HttpContext): Observable<StrictHttpResponse<OfferResponseDto>> {
  const rb = new RequestBuilder(rootUrl, offersControllerEnable.PATH, 'patch');
  if (params) {
    rb.path('id', params.id, {});
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<OfferResponseDto>;
    })
  );
}

offersControllerEnable.PATH = '/api/v1/offers/{id}/enable';
