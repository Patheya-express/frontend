/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { CreateOfferDto } from '../../models/create-offer-dto';
import { OfferResponseDto } from '../../models/offer-response-dto';

export interface OffersControllerCreate$Params {
      body: CreateOfferDto
}

export function offersControllerCreate(http: HttpClient, rootUrl: string, params: OffersControllerCreate$Params, context?: HttpContext): Observable<StrictHttpResponse<OfferResponseDto>> {
  const rb = new RequestBuilder(rootUrl, offersControllerCreate.PATH, 'post');
  if (params) {
    rb.body(params.body, 'application/json');
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<OfferResponseDto>;
    })
  );
}

offersControllerCreate.PATH = '/api/v1/offers';
