/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { PaginatedOffersResponseDto } from '../../models/paginated-offers-response-dto';

export interface OffersControllerFindAll$Params {
  page?: number;
  limit?: number;

/**
 * Matches against title/description
 */
  search?: string;
  restaurantId?: string;
  type?: 'PERCENTAGE_OFF' | 'FLAT_OFF' | 'FREE_DELIVERY' | 'BUY_ONE_GET_ONE' | 'CASHBACK' | 'OTHER';
}

export function offersControllerFindAll(http: HttpClient, rootUrl: string, params?: OffersControllerFindAll$Params, context?: HttpContext): Observable<StrictHttpResponse<PaginatedOffersResponseDto>> {
  const rb = new RequestBuilder(rootUrl, offersControllerFindAll.PATH, 'get');
  if (params) {
    rb.query('page', params.page, {});
    rb.query('limit', params.limit, {});
    rb.query('search', params.search, {});
    rb.query('restaurantId', params.restaurantId, {});
    rb.query('type', params.type, {});
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<PaginatedOffersResponseDto>;
    })
  );
}

offersControllerFindAll.PATH = '/api/v1/offers';
