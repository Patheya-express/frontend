/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { MarkRestaurantArrivalDto } from '../../models/mark-restaurant-arrival-dto';
import { RestaurantArrivalResponseDto } from '../../models/restaurant-arrival-response-dto';

export interface ProofControllerMarkRestaurantArrival$Params {
  orderId: string;
      body: MarkRestaurantArrivalDto
}

export function proofControllerMarkRestaurantArrival(http: HttpClient, rootUrl: string, params: ProofControllerMarkRestaurantArrival$Params, context?: HttpContext): Observable<StrictHttpResponse<RestaurantArrivalResponseDto>> {
  const rb = new RequestBuilder(rootUrl, proofControllerMarkRestaurantArrival.PATH, 'post');
  if (params) {
    rb.path('orderId', params.orderId, {});
    rb.body(params.body, 'application/json');
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<RestaurantArrivalResponseDto>;
    })
  );
}

proofControllerMarkRestaurantArrival.PATH = '/api/v1/delivery/orders/{orderId}/proof/pickup/arrival';
