/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { ProofPhotoResponseDto } from '../../models/proof-photo-response-dto';

export interface ProofControllerGetPickupPhoto$Params {
  orderId: string;
}

export function proofControllerGetPickupPhoto(http: HttpClient, rootUrl: string, params: ProofControllerGetPickupPhoto$Params, context?: HttpContext): Observable<StrictHttpResponse<ProofPhotoResponseDto>> {
  const rb = new RequestBuilder(rootUrl, proofControllerGetPickupPhoto.PATH, 'get');
  if (params) {
    rb.path('orderId', params.orderId, {});
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<ProofPhotoResponseDto>;
    })
  );
}

proofControllerGetPickupPhoto.PATH = '/api/v1/delivery/orders/{orderId}/proof/pickup/photo';
