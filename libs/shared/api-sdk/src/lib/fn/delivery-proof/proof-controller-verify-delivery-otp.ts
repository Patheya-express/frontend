/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { ProofOtpStatusResponseDto } from '../../models/proof-otp-status-response-dto';
import { VerifyProofOtpDto } from '../../models/verify-proof-otp-dto';

export interface ProofControllerVerifyDeliveryOtp$Params {
  orderId: string;
      body: VerifyProofOtpDto
}

export function proofControllerVerifyDeliveryOtp(http: HttpClient, rootUrl: string, params: ProofControllerVerifyDeliveryOtp$Params, context?: HttpContext): Observable<StrictHttpResponse<ProofOtpStatusResponseDto>> {
  const rb = new RequestBuilder(rootUrl, proofControllerVerifyDeliveryOtp.PATH, 'post');
  if (params) {
    rb.path('orderId', params.orderId, {});
    rb.body(params.body, 'application/json');
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<ProofOtpStatusResponseDto>;
    })
  );
}

proofControllerVerifyDeliveryOtp.PATH = '/api/v1/delivery/orders/{orderId}/proof/delivery/verify';
