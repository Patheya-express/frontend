/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { ProofOtpStatusResponseDto } from '../../models/proof-otp-status-response-dto';

export interface ProofControllerGetPickupStatus$Params {
  orderId: string;
}

export function proofControllerGetPickupStatus(http: HttpClient, rootUrl: string, params: ProofControllerGetPickupStatus$Params, context?: HttpContext): Observable<StrictHttpResponse<ProofOtpStatusResponseDto>> {
  const rb = new RequestBuilder(rootUrl, proofControllerGetPickupStatus.PATH, 'get');
  if (params) {
    rb.path('orderId', params.orderId, {});
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<ProofOtpStatusResponseDto>;
    })
  );
}

proofControllerGetPickupStatus.PATH = '/api/v1/delivery/orders/{orderId}/proof/pickup/status';
