/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { ProofPhotoResponseDto } from '../../models/proof-photo-response-dto';

export interface ProofControllerUploadPickupPhoto$Params {
  orderId: string;
      body: {
'file'?: Blob;
}
}

export function proofControllerUploadPickupPhoto(http: HttpClient, rootUrl: string, params: ProofControllerUploadPickupPhoto$Params, context?: HttpContext): Observable<StrictHttpResponse<ProofPhotoResponseDto>> {
  const rb = new RequestBuilder(rootUrl, proofControllerUploadPickupPhoto.PATH, 'post');
  if (params) {
    rb.path('orderId', params.orderId, {});
    rb.body(params.body, 'multipart/form-data');
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<ProofPhotoResponseDto>;
    })
  );
}

proofControllerUploadPickupPhoto.PATH = '/api/v1/delivery/orders/{orderId}/proof/pickup/photo';
