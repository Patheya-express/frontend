/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { MenuCategoryResponseDto } from '../../models/menu-category-response-dto';
import { UpdateCategoryDto } from '../../models/update-category-dto';

export interface MenuControllerUpdateCategory$Params {
  id: string;
      body: UpdateCategoryDto
}

export function menuControllerUpdateCategory(http: HttpClient, rootUrl: string, params: MenuControllerUpdateCategory$Params, context?: HttpContext): Observable<StrictHttpResponse<MenuCategoryResponseDto>> {
  const rb = new RequestBuilder(rootUrl, menuControllerUpdateCategory.PATH, 'patch');
  if (params) {
    rb.path('id', params.id, {});
    rb.body(params.body, 'application/json');
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<MenuCategoryResponseDto>;
    })
  );
}

menuControllerUpdateCategory.PATH = '/api/v1/menu/categories/{id}';
