/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { CreateMenuItemDto } from '../../models/create-menu-item-dto';
import { MenuItemResponseDto } from '../../models/menu-item-response-dto';

export interface MenuControllerCreateMenuItem$Params {
      body: CreateMenuItemDto
}

export function menuControllerCreateMenuItem(http: HttpClient, rootUrl: string, params: MenuControllerCreateMenuItem$Params, context?: HttpContext): Observable<StrictHttpResponse<MenuItemResponseDto>> {
  const rb = new RequestBuilder(rootUrl, menuControllerCreateMenuItem.PATH, 'post');
  if (params) {
    rb.body(params.body, 'application/json');
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<MenuItemResponseDto>;
    })
  );
}

menuControllerCreateMenuItem.PATH = '/api/v1/menu/items';
