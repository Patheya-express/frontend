/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { CreateAddonOptionDto } from '../../models/create-addon-option-dto';
import { MenuAddonOptionResponseDto } from '../../models/menu-addon-option-response-dto';

export interface MenuControllerCreateAddonOption$Params {
  id: string;
      body: CreateAddonOptionDto
}

export function menuControllerCreateAddonOption(http: HttpClient, rootUrl: string, params: MenuControllerCreateAddonOption$Params, context?: HttpContext): Observable<StrictHttpResponse<MenuAddonOptionResponseDto>> {
  const rb = new RequestBuilder(rootUrl, menuControllerCreateAddonOption.PATH, 'post');
  if (params) {
    rb.path('id', params.id, {});
    rb.body(params.body, 'application/json');
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<MenuAddonOptionResponseDto>;
    })
  );
}

menuControllerCreateAddonOption.PATH = '/api/v1/menu/addons/{id}/options';
