/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { MenuAddonOptionResponseDto } from '../../models/menu-addon-option-response-dto';
import { UpdateAddonOptionDto } from '../../models/update-addon-option-dto';

export interface MenuControllerUpdateAddonOption$Params {
  id: string;
      body: UpdateAddonOptionDto
}

export function menuControllerUpdateAddonOption(http: HttpClient, rootUrl: string, params: MenuControllerUpdateAddonOption$Params, context?: HttpContext): Observable<StrictHttpResponse<MenuAddonOptionResponseDto>> {
  const rb = new RequestBuilder(rootUrl, menuControllerUpdateAddonOption.PATH, 'patch');
  if (params) {
    rb.path('id', params.id, {});
    rb.body(params.body, 'application/json');
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<MenuAddonOptionResponseDto>;
    })
  );
}

menuControllerUpdateAddonOption.PATH = '/api/v1/menu/options/{id}';
