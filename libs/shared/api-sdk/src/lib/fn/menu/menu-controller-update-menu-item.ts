/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { MenuItemResponseDto } from '../../models/menu-item-response-dto';
import { UpdateMenuItemDto } from '../../models/update-menu-item-dto';

export interface MenuControllerUpdateMenuItem$Params {
  id: string;
      body: UpdateMenuItemDto
}

export function menuControllerUpdateMenuItem(http: HttpClient, rootUrl: string, params: MenuControllerUpdateMenuItem$Params, context?: HttpContext): Observable<StrictHttpResponse<MenuItemResponseDto>> {
  const rb = new RequestBuilder(rootUrl, menuControllerUpdateMenuItem.PATH, 'patch');
  if (params) {
    rb.path('id', params.id, {});
    rb.body(params.body, 'application/json');
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<MenuItemResponseDto>;
    })
  );
}

menuControllerUpdateMenuItem.PATH = '/api/v1/menu/items/{id}';
