/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { MenuItemVariantResponseDto } from '../../models/menu-item-variant-response-dto';
import { UpdateMenuItemVariantDto } from '../../models/update-menu-item-variant-dto';

export interface MenuControllerUpdateVariant$Params {
  id: string;
      body: UpdateMenuItemVariantDto
}

export function menuControllerUpdateVariant(http: HttpClient, rootUrl: string, params: MenuControllerUpdateVariant$Params, context?: HttpContext): Observable<StrictHttpResponse<MenuItemVariantResponseDto>> {
  const rb = new RequestBuilder(rootUrl, menuControllerUpdateVariant.PATH, 'patch');
  if (params) {
    rb.path('id', params.id, {});
    rb.body(params.body, 'application/json');
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<MenuItemVariantResponseDto>;
    })
  );
}

menuControllerUpdateVariant.PATH = '/api/v1/menu/variants/{id}';
