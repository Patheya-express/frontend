/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { DispatchActionResponseDto } from '../../models/dispatch-action-response-dto';

export interface DispatchControllerAcceptAssignment$Params {

/**
 * Delivery assignment ID
 */
  id: string;
}

export function dispatchControllerAcceptAssignment(http: HttpClient, rootUrl: string, params: DispatchControllerAcceptAssignment$Params, context?: HttpContext): Observable<StrictHttpResponse<DispatchActionResponseDto>> {
  const rb = new RequestBuilder(rootUrl, dispatchControllerAcceptAssignment.PATH, 'patch');
  if (params) {
    rb.path('id', params.id, {});
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<DispatchActionResponseDto>;
    })
  );
}

dispatchControllerAcceptAssignment.PATH = '/api/v1/dispatch/assignments/{id}/accept';
