/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { DispatchActionResponseDto } from '../../models/dispatch-action-response-dto';

export interface DispatchControllerRejectAssignment$Params {

/**
 * Delivery assignment ID
 */
  id: string;
}

export function dispatchControllerRejectAssignment(http: HttpClient, rootUrl: string, params: DispatchControllerRejectAssignment$Params, context?: HttpContext): Observable<StrictHttpResponse<DispatchActionResponseDto>> {
  const rb = new RequestBuilder(rootUrl, dispatchControllerRejectAssignment.PATH, 'patch');
  if (params) {
    rb.path('id', params.id, {});
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<DispatchActionResponseDto>;
    })
  );
}

dispatchControllerRejectAssignment.PATH = '/api/v1/dispatch/assignments/{id}/reject';
