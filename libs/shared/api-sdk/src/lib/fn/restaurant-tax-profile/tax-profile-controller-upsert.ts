/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { TaxProfileResponseDto } from '../../models/tax-profile-response-dto';
import { UpsertTaxProfileDto } from '../../models/upsert-tax-profile-dto';

export interface TaxProfileControllerUpsert$Params {
  restaurantId: string;
      body: UpsertTaxProfileDto
}

export function taxProfileControllerUpsert(http: HttpClient, rootUrl: string, params: TaxProfileControllerUpsert$Params, context?: HttpContext): Observable<StrictHttpResponse<TaxProfileResponseDto>> {
  const rb = new RequestBuilder(rootUrl, taxProfileControllerUpsert.PATH, 'patch');
  if (params) {
    rb.path('restaurantId', params.restaurantId, {});
    rb.body(params.body, 'application/json');
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<TaxProfileResponseDto>;
    })
  );
}

taxProfileControllerUpsert.PATH = '/api/v1/restaurants/{restaurantId}/tax-profile';
