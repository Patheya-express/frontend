/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { TaxProfileResponseDto } from '../../models/tax-profile-response-dto';

export interface TaxProfileControllerFind$Params {
  restaurantId: string;
}

export function taxProfileControllerFind(http: HttpClient, rootUrl: string, params: TaxProfileControllerFind$Params, context?: HttpContext): Observable<StrictHttpResponse<TaxProfileResponseDto>> {
  const rb = new RequestBuilder(rootUrl, taxProfileControllerFind.PATH, 'get');
  if (params) {
    rb.path('restaurantId', params.restaurantId, {});
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<TaxProfileResponseDto>;
    })
  );
}

taxProfileControllerFind.PATH = '/api/v1/restaurants/{restaurantId}/tax-profile';
