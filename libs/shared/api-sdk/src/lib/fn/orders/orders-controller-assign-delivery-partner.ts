/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { AssignDeliveryPartnerDto } from '../../models/assign-delivery-partner-dto';
import { OrderResponseDto } from '../../models/order-response-dto';

export interface OrdersControllerAssignDeliveryPartner$Params {
  id: string;
      body: AssignDeliveryPartnerDto
}

export function ordersControllerAssignDeliveryPartner(http: HttpClient, rootUrl: string, params: OrdersControllerAssignDeliveryPartner$Params, context?: HttpContext): Observable<StrictHttpResponse<OrderResponseDto>> {
  const rb = new RequestBuilder(rootUrl, ordersControllerAssignDeliveryPartner.PATH, 'post');
  if (params) {
    rb.path('id', params.id, {});
    rb.body(params.body, 'application/json');
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<OrderResponseDto>;
    })
  );
}

ordersControllerAssignDeliveryPartner.PATH = '/api/v1/orders/{id}/assign-delivery-partner';
