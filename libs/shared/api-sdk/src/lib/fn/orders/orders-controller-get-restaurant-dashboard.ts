/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { RestaurantDashboardResponseDto } from '../../models/restaurant-dashboard-response-dto';

export interface OrdersControllerGetRestaurantDashboard$Params {
  restaurantId: string;

/**
 * Scope every metric to a single branch.
 */
  branchId?: string;
}

export function ordersControllerGetRestaurantDashboard(http: HttpClient, rootUrl: string, params: OrdersControllerGetRestaurantDashboard$Params, context?: HttpContext): Observable<StrictHttpResponse<RestaurantDashboardResponseDto>> {
  const rb = new RequestBuilder(rootUrl, ordersControllerGetRestaurantDashboard.PATH, 'get');
  if (params) {
    rb.path('restaurantId', params.restaurantId, {});
    rb.query('branchId', params.branchId, {});
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<RestaurantDashboardResponseDto>;
    })
  );
}

ordersControllerGetRestaurantDashboard.PATH = '/api/v1/orders/restaurant/{restaurantId}/dashboard';
