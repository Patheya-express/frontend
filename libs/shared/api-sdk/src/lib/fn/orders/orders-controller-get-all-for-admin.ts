/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { PaginatedAdminOrdersResponseDto } from '../../models/paginated-admin-orders-response-dto';

export interface OrdersControllerGetAllForAdmin$Params {

/**
 * ISO date — filters orders placed on or before this date
 */
  dateTo?: any;

/**
 * ISO date — filters orders placed on or after this date
 */
  dateFrom?: any;
  deliveryPartnerId?: any;
  customerId?: any;
  restaurantId?: any;
  status?: 'PENDING' | 'CONFIRMED' | 'PREPARING' | 'READY_FOR_PICKUP' | 'OUT_FOR_DELIVERY' | 'DELIVERED' | 'CANCELLED';

/**
 * Matches order number, customer name/email, or restaurant name
 */
  search?: any;
  limit?: number;
  page?: number;
}

export function ordersControllerGetAllForAdmin(http: HttpClient, rootUrl: string, params?: OrdersControllerGetAllForAdmin$Params, context?: HttpContext): Observable<StrictHttpResponse<PaginatedAdminOrdersResponseDto>> {
  const rb = new RequestBuilder(rootUrl, ordersControllerGetAllForAdmin.PATH, 'get');
  if (params) {
    rb.query('dateTo', params.dateTo, {});
    rb.query('dateFrom', params.dateFrom, {});
    rb.query('deliveryPartnerId', params.deliveryPartnerId, {});
    rb.query('customerId', params.customerId, {});
    rb.query('restaurantId', params.restaurantId, {});
    rb.query('status', params.status, {});
    rb.query('search', params.search, {});
    rb.query('limit', params.limit, {});
    rb.query('page', params.page, {});
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<PaginatedAdminOrdersResponseDto>;
    })
  );
}

ordersControllerGetAllForAdmin.PATH = '/api/v1/orders/admin';
