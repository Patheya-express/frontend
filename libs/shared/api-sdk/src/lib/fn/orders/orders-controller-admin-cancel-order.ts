/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { CancelOrderDto } from '../../models/cancel-order-dto';
import { OrderResponseDto } from '../../models/order-response-dto';

export interface OrdersControllerAdminCancelOrder$Params {
  id: string;
      body: CancelOrderDto
}

export function ordersControllerAdminCancelOrder(http: HttpClient, rootUrl: string, params: OrdersControllerAdminCancelOrder$Params, context?: HttpContext): Observable<StrictHttpResponse<OrderResponseDto>> {
  const rb = new RequestBuilder(rootUrl, ordersControllerAdminCancelOrder.PATH, 'patch');
  if (params) {
    rb.path('id', params.id, {});
    rb.body(params.body, 'application/json');
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<OrderResponseDto>;
    })
  );
}

ordersControllerAdminCancelOrder.PATH = '/api/v1/orders/{id}/admin-cancel';
