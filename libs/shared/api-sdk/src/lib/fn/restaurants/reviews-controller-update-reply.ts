/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { ReplyDto } from '../../models/reply-dto';
import { ReviewResponseDto } from '../../models/review-response-dto';

export interface ReviewsControllerUpdateReply$Params {
  id: string;
  reviewId: string;
      body: ReplyDto
}

export function reviewsControllerUpdateReply(http: HttpClient, rootUrl: string, params: ReviewsControllerUpdateReply$Params, context?: HttpContext): Observable<StrictHttpResponse<ReviewResponseDto>> {
  const rb = new RequestBuilder(rootUrl, reviewsControllerUpdateReply.PATH, 'patch');
  if (params) {
    rb.path('id', params.id, {});
    rb.path('reviewId', params.reviewId, {});
    rb.body(params.body, 'application/json');
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<ReviewResponseDto>;
    })
  );
}

reviewsControllerUpdateReply.PATH = '/api/v1/restaurants/{id}/reviews/{reviewId}/reply';
