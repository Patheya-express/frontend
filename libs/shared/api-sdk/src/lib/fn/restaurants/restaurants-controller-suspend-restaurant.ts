/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { RestaurantResponseDto } from '../../models/restaurant-response-dto';

export interface RestaurantsControllerSuspendRestaurant$Params {
  id: string;
}

export function restaurantsControllerSuspendRestaurant(http: HttpClient, rootUrl: string, params: RestaurantsControllerSuspendRestaurant$Params, context?: HttpContext): Observable<StrictHttpResponse<RestaurantResponseDto>> {
  const rb = new RequestBuilder(rootUrl, restaurantsControllerSuspendRestaurant.PATH, 'patch');
  if (params) {
    rb.path('id', params.id, {});
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<RestaurantResponseDto>;
    })
  );
}

restaurantsControllerSuspendRestaurant.PATH = '/api/v1/restaurants/{id}/suspend';
