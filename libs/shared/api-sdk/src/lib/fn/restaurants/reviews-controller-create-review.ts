/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { CreateReviewDto } from '../../models/create-review-dto';
import { ReviewResponseDto } from '../../models/review-response-dto';

export interface ReviewsControllerCreateReview$Params {
  id: string;
      body: CreateReviewDto
}

export function reviewsControllerCreateReview(http: HttpClient, rootUrl: string, params: ReviewsControllerCreateReview$Params, context?: HttpContext): Observable<StrictHttpResponse<ReviewResponseDto>> {
  const rb = new RequestBuilder(rootUrl, reviewsControllerCreateReview.PATH, 'post');
  if (params) {
    rb.path('id', params.id, {});
    rb.body(params.body, 'application/json');
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<ReviewResponseDto>;
    })
  );
}

reviewsControllerCreateReview.PATH = '/api/v1/restaurants/{id}/reviews';
