/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { ReviewResponseDto } from '../../models/review-response-dto';

export interface ReviewsControllerGetMyReview$Params {
  id: string;
}

export function reviewsControllerGetMyReview(http: HttpClient, rootUrl: string, params: ReviewsControllerGetMyReview$Params, context?: HttpContext): Observable<StrictHttpResponse<ReviewResponseDto>> {
  const rb = new RequestBuilder(rootUrl, reviewsControllerGetMyReview.PATH, 'get');
  if (params) {
    rb.path('id', params.id, {});
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<ReviewResponseDto>;
    })
  );
}

reviewsControllerGetMyReview.PATH = '/api/v1/restaurants/{id}/reviews/me';
