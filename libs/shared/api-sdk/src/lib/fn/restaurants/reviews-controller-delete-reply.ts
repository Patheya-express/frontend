/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';


export interface ReviewsControllerDeleteReply$Params {
  id: string;
  reviewId: string;
}

export function reviewsControllerDeleteReply(http: HttpClient, rootUrl: string, params: ReviewsControllerDeleteReply$Params, context?: HttpContext): Observable<StrictHttpResponse<void>> {
  const rb = new RequestBuilder(rootUrl, reviewsControllerDeleteReply.PATH, 'delete');
  if (params) {
    rb.path('id', params.id, {});
    rb.path('reviewId', params.reviewId, {});
  }

  return http.request(
    rb.build({ responseType: 'text', accept: '*/*', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return (r as HttpResponse<any>).clone({ body: undefined }) as StrictHttpResponse<void>;
    })
  );
}

reviewsControllerDeleteReply.PATH = '/api/v1/restaurants/{id}/reviews/{reviewId}/reply';
