/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { RatingSummaryResponseDto } from '../../models/rating-summary-response-dto';

export interface ReviewsControllerGetRatingSummary$Params {
  id: string;
}

export function reviewsControllerGetRatingSummary(http: HttpClient, rootUrl: string, params: ReviewsControllerGetRatingSummary$Params, context?: HttpContext): Observable<StrictHttpResponse<RatingSummaryResponseDto>> {
  const rb = new RequestBuilder(rootUrl, reviewsControllerGetRatingSummary.PATH, 'get');
  if (params) {
    rb.path('id', params.id, {});
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<RatingSummaryResponseDto>;
    })
  );
}

reviewsControllerGetRatingSummary.PATH = '/api/v1/restaurants/{id}/reviews/manage/summary';
