/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { ReplyDto } from '../../models/reply-dto';
import { ReviewResponseDto } from '../../models/review-response-dto';

export interface ReviewsControllerCreateReply$Params {
  id: string;
  reviewId: string;
      body: ReplyDto
}

export function reviewsControllerCreateReply(http: HttpClient, rootUrl: string, params: ReviewsControllerCreateReply$Params, context?: HttpContext): Observable<StrictHttpResponse<ReviewResponseDto>> {
  const rb = new RequestBuilder(rootUrl, reviewsControllerCreateReply.PATH, 'post');
  if (params) {
    rb.path('id', params.id, {});
    rb.path('reviewId', params.reviewId, {});
    rb.body(params.body, 'application/json');
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<ReviewResponseDto>;
    })
  );
}

reviewsControllerCreateReply.PATH = '/api/v1/restaurants/{id}/reviews/{reviewId}/reply';
