/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { PaginatedReviewsResponseDto } from '../../models/paginated-reviews-response-dto';

export interface ReviewsControllerGetReviewsForManage$Params {
  id: string;
  page?: number;
  limit?: number;

/**
 * Filter to a single star rating (1-5).
 */
  rating?: number;

/**
 * Only reviews created on or after this date.
 */
  dateFrom?: string;

/**
 * Only reviews created on or before this date.
 */
  dateTo?: string;

/**
 * Filter to only reviews that have (or have not) been replied to. Omit for both.
 */
  hasReply?: boolean;
}

export function reviewsControllerGetReviewsForManage(http: HttpClient, rootUrl: string, params: ReviewsControllerGetReviewsForManage$Params, context?: HttpContext): Observable<StrictHttpResponse<PaginatedReviewsResponseDto>> {
  const rb = new RequestBuilder(rootUrl, reviewsControllerGetReviewsForManage.PATH, 'get');
  if (params) {
    rb.path('id', params.id, {});
    rb.query('page', params.page, {});
    rb.query('limit', params.limit, {});
    rb.query('rating', params.rating, {});
    rb.query('dateFrom', params.dateFrom, {});
    rb.query('dateTo', params.dateTo, {});
    rb.query('hasReply', params.hasReply, {});
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<PaginatedReviewsResponseDto>;
    })
  );
}

reviewsControllerGetReviewsForManage.PATH = '/api/v1/restaurants/{id}/reviews/manage';
