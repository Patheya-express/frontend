/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { RestaurantResponseDto } from '../../models/restaurant-response-dto';

export interface RestaurantsControllerGetMyRestaurants$Params {
}

export function restaurantsControllerGetMyRestaurants(http: HttpClient, rootUrl: string, params?: RestaurantsControllerGetMyRestaurants$Params, context?: HttpContext): Observable<StrictHttpResponse<Array<RestaurantResponseDto>>> {
  const rb = new RequestBuilder(rootUrl, restaurantsControllerGetMyRestaurants.PATH, 'get');
  if (params) {
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<Array<RestaurantResponseDto>>;
    })
  );
}

restaurantsControllerGetMyRestaurants.PATH = '/api/v1/restaurants/me';
