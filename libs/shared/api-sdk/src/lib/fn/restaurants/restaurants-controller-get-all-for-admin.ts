/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { PaginatedRestaurantsResponseDto } from '../../models/paginated-restaurants-response-dto';

export interface RestaurantsControllerGetAllForAdmin$Params {
  status?: 'PENDING' | 'APPROVED' | 'REJECTED' | 'SUSPENDED';
  cuisine?: any;
  city?: any;

/**
 * Matches restaurant name, owner first name, owner last name, or owner email
 */
  search?: any;
  limit?: number;
  page?: number;
}

export function restaurantsControllerGetAllForAdmin(http: HttpClient, rootUrl: string, params?: RestaurantsControllerGetAllForAdmin$Params, context?: HttpContext): Observable<StrictHttpResponse<PaginatedRestaurantsResponseDto>> {
  const rb = new RequestBuilder(rootUrl, restaurantsControllerGetAllForAdmin.PATH, 'get');
  if (params) {
    rb.query('status', params.status, {});
    rb.query('cuisine', params.cuisine, {});
    rb.query('city', params.city, {});
    rb.query('search', params.search, {});
    rb.query('limit', params.limit, {});
    rb.query('page', params.page, {});
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<PaginatedRestaurantsResponseDto>;
    })
  );
}

restaurantsControllerGetAllForAdmin.PATH = '/api/v1/restaurants/admin';
