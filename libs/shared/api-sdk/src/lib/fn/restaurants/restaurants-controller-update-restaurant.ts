/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { RestaurantResponseDto } from '../../models/restaurant-response-dto';
import { UpdateRestaurantDto } from '../../models/update-restaurant-dto';

export interface RestaurantsControllerUpdateRestaurant$Params {
  id: string;
      body: UpdateRestaurantDto
}

export function restaurantsControllerUpdateRestaurant(http: HttpClient, rootUrl: string, params: RestaurantsControllerUpdateRestaurant$Params, context?: HttpContext): Observable<StrictHttpResponse<RestaurantResponseDto>> {
  const rb = new RequestBuilder(rootUrl, restaurantsControllerUpdateRestaurant.PATH, 'patch');
  if (params) {
    rb.path('id', params.id, {});
    rb.body(params.body, 'application/json');
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<RestaurantResponseDto>;
    })
  );
}

restaurantsControllerUpdateRestaurant.PATH = '/api/v1/restaurants/{id}';
