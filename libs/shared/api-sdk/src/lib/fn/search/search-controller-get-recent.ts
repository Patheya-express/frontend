/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { RecentSearchDto } from '../../models/recent-search-dto';

export interface SearchControllerGetRecent$Params {
}

export function searchControllerGetRecent(http: HttpClient, rootUrl: string, params?: SearchControllerGetRecent$Params, context?: HttpContext): Observable<StrictHttpResponse<Array<RecentSearchDto>>> {
  const rb = new RequestBuilder(rootUrl, searchControllerGetRecent.PATH, 'get');
  if (params) {
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<Array<RecentSearchDto>>;
    })
  );
}

searchControllerGetRecent.PATH = '/api/v1/search/recent';
