/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { GlobalSearchResponseDto } from '../../models/global-search-response-dto';

export interface SearchControllerGlobalSearch$Params {
  limit?: number;
  q: any;
}

export function searchControllerGlobalSearch(http: HttpClient, rootUrl: string, params: SearchControllerGlobalSearch$Params, context?: HttpContext): Observable<StrictHttpResponse<GlobalSearchResponseDto>> {
  const rb = new RequestBuilder(rootUrl, searchControllerGlobalSearch.PATH, 'get');
  if (params) {
    rb.query('limit', params.limit, {});
    rb.query('q', params.q, {});
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<GlobalSearchResponseDto>;
    })
  );
}

searchControllerGlobalSearch.PATH = '/api/v1/search/global';
