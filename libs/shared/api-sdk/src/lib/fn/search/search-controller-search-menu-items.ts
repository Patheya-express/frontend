/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { PaginatedMenuItemSearchResponseDto } from '../../models/paginated-menu-item-search-response-dto';

export interface SearchControllerSearchMenuItems$Params {
  limit?: number;
  page?: number;
  q: any;
}

export function searchControllerSearchMenuItems(http: HttpClient, rootUrl: string, params: SearchControllerSearchMenuItems$Params, context?: HttpContext): Observable<StrictHttpResponse<PaginatedMenuItemSearchResponseDto>> {
  const rb = new RequestBuilder(rootUrl, searchControllerSearchMenuItems.PATH, 'get');
  if (params) {
    rb.query('limit', params.limit, {});
    rb.query('page', params.page, {});
    rb.query('q', params.q, {});
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<PaginatedMenuItemSearchResponseDto>;
    })
  );
}

searchControllerSearchMenuItems.PATH = '/api/v1/search/menu-items';
