/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { WalletBalanceResponseDto } from '../../models/wallet-balance-response-dto';

export interface WalletControllerGetBalance$Params {
}

export function walletControllerGetBalance(http: HttpClient, rootUrl: string, params?: WalletControllerGetBalance$Params, context?: HttpContext): Observable<StrictHttpResponse<WalletBalanceResponseDto>> {
  const rb = new RequestBuilder(rootUrl, walletControllerGetBalance.PATH, 'get');
  if (params) {
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<WalletBalanceResponseDto>;
    })
  );
}

walletControllerGetBalance.PATH = '/api/v1/wallet/balance';
