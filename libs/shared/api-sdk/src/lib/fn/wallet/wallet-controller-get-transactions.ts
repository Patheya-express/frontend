/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { PaginatedWalletTransactionsResponseDto } from '../../models/paginated-wallet-transactions-response-dto';

export interface WalletControllerGetTransactions$Params {
  limit?: number;
  page?: number;
}

export function walletControllerGetTransactions(http: HttpClient, rootUrl: string, params?: WalletControllerGetTransactions$Params, context?: HttpContext): Observable<StrictHttpResponse<PaginatedWalletTransactionsResponseDto>> {
  const rb = new RequestBuilder(rootUrl, walletControllerGetTransactions.PATH, 'get');
  if (params) {
    rb.query('limit', params.limit, {});
    rb.query('page', params.page, {});
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<PaginatedWalletTransactionsResponseDto>;
    })
  );
}

walletControllerGetTransactions.PATH = '/api/v1/wallet/transactions';
