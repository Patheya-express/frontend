/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';


export interface StaffControllerRevoke$Params {
  restaurantId: string;
  staffId: string;
}

export function staffControllerRevoke(http: HttpClient, rootUrl: string, params: StaffControllerRevoke$Params, context?: HttpContext): Observable<StrictHttpResponse<void>> {
  const rb = new RequestBuilder(rootUrl, staffControllerRevoke.PATH, 'delete');
  if (params) {
    rb.path('restaurantId', params.restaurantId, {});
    rb.path('staffId', params.staffId, {});
  }

  return http.request(
    rb.build({ responseType: 'text', accept: '*/*', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return (r as HttpResponse<any>).clone({ body: undefined }) as StrictHttpResponse<void>;
    })
  );
}

staffControllerRevoke.PATH = '/api/v1/restaurants/{restaurantId}/staff/{staffId}';
