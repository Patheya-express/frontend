/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { InviteStaffDto } from '../../models/invite-staff-dto';
import { StaffResponseDto } from '../../models/staff-response-dto';

export interface StaffControllerInvite$Params {
  restaurantId: string;
      body: InviteStaffDto
}

export function staffControllerInvite(http: HttpClient, rootUrl: string, params: StaffControllerInvite$Params, context?: HttpContext): Observable<StrictHttpResponse<StaffResponseDto>> {
  const rb = new RequestBuilder(rootUrl, staffControllerInvite.PATH, 'post');
  if (params) {
    rb.path('restaurantId', params.restaurantId, {});
    rb.body(params.body, 'application/json');
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<StaffResponseDto>;
    })
  );
}

staffControllerInvite.PATH = '/api/v1/restaurants/{restaurantId}/staff';
