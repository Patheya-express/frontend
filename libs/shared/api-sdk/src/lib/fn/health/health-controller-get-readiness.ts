/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { ReadinessResponseDto } from '../../models/readiness-response-dto';

export interface HealthControllerGetReadiness$Params {
}

export function healthControllerGetReadiness(http: HttpClient, rootUrl: string, params?: HealthControllerGetReadiness$Params, context?: HttpContext): Observable<StrictHttpResponse<ReadinessResponseDto>> {
  const rb = new RequestBuilder(rootUrl, healthControllerGetReadiness.PATH, 'get');
  if (params) {
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<ReadinessResponseDto>;
    })
  );
}

healthControllerGetReadiness.PATH = '/api/v1/health/ready';
