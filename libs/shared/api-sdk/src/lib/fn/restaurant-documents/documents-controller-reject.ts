/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { DocumentResponseDto } from '../../models/document-response-dto';
import { RejectDocumentDto } from '../../models/reject-document-dto';

export interface DocumentsControllerReject$Params {
  restaurantId: string;
  documentId: string;
      body: RejectDocumentDto
}

export function documentsControllerReject(http: HttpClient, rootUrl: string, params: DocumentsControllerReject$Params, context?: HttpContext): Observable<StrictHttpResponse<DocumentResponseDto>> {
  const rb = new RequestBuilder(rootUrl, documentsControllerReject.PATH, 'patch');
  if (params) {
    rb.path('restaurantId', params.restaurantId, {});
    rb.path('documentId', params.documentId, {});
    rb.body(params.body, 'application/json');
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<DocumentResponseDto>;
    })
  );
}

documentsControllerReject.PATH = '/api/v1/restaurants/{restaurantId}/documents/{documentId}/reject';
