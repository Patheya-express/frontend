/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { OperatingHourResponseDto } from '../../models/operating-hour-response-dto';
import { ReplaceOperatingHoursDto } from '../../models/replace-operating-hours-dto';

export interface OperatingHoursControllerReplaceAll$Params {
  restaurantId: string;
  branchId: string;
      body: ReplaceOperatingHoursDto
}

export function operatingHoursControllerReplaceAll(http: HttpClient, rootUrl: string, params: OperatingHoursControllerReplaceAll$Params, context?: HttpContext): Observable<StrictHttpResponse<Array<OperatingHourResponseDto>>> {
  const rb = new RequestBuilder(rootUrl, operatingHoursControllerReplaceAll.PATH, 'put');
  if (params) {
    rb.path('restaurantId', params.restaurantId, {});
    rb.path('branchId', params.branchId, {});
    rb.body(params.body, 'application/json');
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<Array<OperatingHourResponseDto>>;
    })
  );
}

operatingHoursControllerReplaceAll.PATH = '/api/v1/restaurants/{restaurantId}/branches/{branchId}/operating-hours';
