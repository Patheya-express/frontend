/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { LogoutResponseDto } from '../../models/logout-response-dto';
import { RefreshTokenDto } from '../../models/refresh-token-dto';

export interface AuthControllerLogout$Params {
      body: RefreshTokenDto
}

export function authControllerLogout(http: HttpClient, rootUrl: string, params: AuthControllerLogout$Params, context?: HttpContext): Observable<StrictHttpResponse<LogoutResponseDto>> {
  const rb = new RequestBuilder(rootUrl, authControllerLogout.PATH, 'post');
  if (params) {
    rb.body(params.body, 'application/json');
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<LogoutResponseDto>;
    })
  );
}

authControllerLogout.PATH = '/api/v1/auth/logout';
