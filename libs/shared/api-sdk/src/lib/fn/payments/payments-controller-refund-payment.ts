/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { RazorpayRefundResponseDto } from '../../models/razorpay-refund-response-dto';
import { RefundPaymentDto } from '../../models/refund-payment-dto';

export interface PaymentsControllerRefundPayment$Params {
      body: RefundPaymentDto
}

export function paymentsControllerRefundPayment(http: HttpClient, rootUrl: string, params: PaymentsControllerRefundPayment$Params, context?: HttpContext): Observable<StrictHttpResponse<RazorpayRefundResponseDto>> {
  const rb = new RequestBuilder(rootUrl, paymentsControllerRefundPayment.PATH, 'post');
  if (params) {
    rb.body(params.body, 'application/json');
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<RazorpayRefundResponseDto>;
    })
  );
}

paymentsControllerRefundPayment.PATH = '/api/v1/payments/refund';
