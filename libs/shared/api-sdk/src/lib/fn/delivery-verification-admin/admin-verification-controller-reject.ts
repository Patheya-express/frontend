/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { DeliveryVerificationResponseDto } from '../../models/delivery-verification-response-dto';
import { RejectDeliveryVerificationDto } from '../../models/reject-delivery-verification-dto';

export interface AdminVerificationControllerReject$Params {
  deliveryPartnerId: string;
      body: RejectDeliveryVerificationDto
}

export function adminVerificationControllerReject(http: HttpClient, rootUrl: string, params: AdminVerificationControllerReject$Params, context?: HttpContext): Observable<StrictHttpResponse<DeliveryVerificationResponseDto>> {
  const rb = new RequestBuilder(rootUrl, adminVerificationControllerReject.PATH, 'patch');
  if (params) {
    rb.path('deliveryPartnerId', params.deliveryPartnerId, {});
    rb.body(params.body, 'application/json');
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<DeliveryVerificationResponseDto>;
    })
  );
}

adminVerificationControllerReject.PATH = '/api/v1/delivery/admin/{deliveryPartnerId}/verification/reject';
