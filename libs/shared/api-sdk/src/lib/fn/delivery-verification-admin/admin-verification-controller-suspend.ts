/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { DeliveryVerificationResponseDto } from '../../models/delivery-verification-response-dto';

export interface AdminVerificationControllerSuspend$Params {
  deliveryPartnerId: string;
}

export function adminVerificationControllerSuspend(http: HttpClient, rootUrl: string, params: AdminVerificationControllerSuspend$Params, context?: HttpContext): Observable<StrictHttpResponse<DeliveryVerificationResponseDto>> {
  const rb = new RequestBuilder(rootUrl, adminVerificationControllerSuspend.PATH, 'patch');
  if (params) {
    rb.path('deliveryPartnerId', params.deliveryPartnerId, {});
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<DeliveryVerificationResponseDto>;
    })
  );
}

adminVerificationControllerSuspend.PATH = '/api/v1/delivery/admin/{deliveryPartnerId}/verification/suspend';
