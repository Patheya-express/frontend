/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { PaginatedAdminAuditLogsResponseDto } from '../../models/paginated-admin-audit-logs-response-dto';

export interface AuditControllerGetAllForAdmin$Params {
  dateTo?: any;
  dateFrom?: any;
  userId?: any;
  entityType?: any;
  action?: 'CREATE' | 'UPDATE' | 'DELETE' | 'LOGIN' | 'LOGOUT' | 'STATUS_CHANGE' | 'APPROVE' | 'REJECT';

/**
 * Matches entity type/ID or actor name/email
 */
  search?: any;
  limit?: number;
  page?: number;
}

export function auditControllerGetAllForAdmin(http: HttpClient, rootUrl: string, params?: AuditControllerGetAllForAdmin$Params, context?: HttpContext): Observable<StrictHttpResponse<PaginatedAdminAuditLogsResponseDto>> {
  const rb = new RequestBuilder(rootUrl, auditControllerGetAllForAdmin.PATH, 'get');
  if (params) {
    rb.query('dateTo', params.dateTo, {});
    rb.query('dateFrom', params.dateFrom, {});
    rb.query('userId', params.userId, {});
    rb.query('entityType', params.entityType, {});
    rb.query('action', params.action, {});
    rb.query('search', params.search, {});
    rb.query('limit', params.limit, {});
    rb.query('page', params.page, {});
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<PaginatedAdminAuditLogsResponseDto>;
    })
  );
}

auditControllerGetAllForAdmin.PATH = '/api/v1/audit/admin';
