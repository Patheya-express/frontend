/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { PaginatedAdminDeliveryPartnersResponseDto } from '../../models/paginated-admin-delivery-partners-response-dto';

export interface DeliveryControllerGetAllForAdmin$Params {
  dateTo?: any;
  dateFrom?: any;

/**
 * Live Redis presence — distinct from `status`
 */
  online?: boolean;

/**
 * true = AVAILABLE only, false = anything else
 */
  availability?: boolean;

/**
 * true = verified only, false = unverified only, omitted = all
 */
  verified?: boolean;
  status?: 'OFFLINE' | 'AVAILABLE' | 'ON_DELIVERY' | 'SUSPENDED';

/**
 * Matches partner name/email/phone or vehicle number
 */
  search?: any;
  limit?: number;
  page?: number;
}

export function deliveryControllerGetAllForAdmin(http: HttpClient, rootUrl: string, params?: DeliveryControllerGetAllForAdmin$Params, context?: HttpContext): Observable<StrictHttpResponse<PaginatedAdminDeliveryPartnersResponseDto>> {
  const rb = new RequestBuilder(rootUrl, deliveryControllerGetAllForAdmin.PATH, 'get');
  if (params) {
    rb.query('dateTo', params.dateTo, {});
    rb.query('dateFrom', params.dateFrom, {});
    rb.query('online', params.online, {});
    rb.query('availability', params.availability, {});
    rb.query('verified', params.verified, {});
    rb.query('status', params.status, {});
    rb.query('search', params.search, {});
    rb.query('limit', params.limit, {});
    rb.query('page', params.page, {});
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<PaginatedAdminDeliveryPartnersResponseDto>;
    })
  );
}

deliveryControllerGetAllForAdmin.PATH = '/api/v1/delivery/admin';
