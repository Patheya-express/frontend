/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { FavoriteToggleResponseDto } from '../../models/favorite-toggle-response-dto';

export interface FavoritesControllerRemoveRestaurantFavorite$Params {
  restaurantId: string;
}

export function favoritesControllerRemoveRestaurantFavorite(http: HttpClient, rootUrl: string, params: FavoritesControllerRemoveRestaurantFavorite$Params, context?: HttpContext): Observable<StrictHttpResponse<FavoriteToggleResponseDto>> {
  const rb = new RequestBuilder(rootUrl, favoritesControllerRemoveRestaurantFavorite.PATH, 'delete');
  if (params) {
    rb.path('restaurantId', params.restaurantId, {});
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<FavoriteToggleResponseDto>;
    })
  );
}

favoritesControllerRemoveRestaurantFavorite.PATH = '/api/v1/favorites/restaurants/{restaurantId}';
