/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { FavoriteStatusItemDto } from '../../models/favorite-status-item-dto';

export interface FavoritesControllerGetRestaurantFavoriteStatus$Params {

/**
 * Comma-separated restaurant ids
 */
  ids: string;
}

export function favoritesControllerGetRestaurantFavoriteStatus(http: HttpClient, rootUrl: string, params: FavoritesControllerGetRestaurantFavoriteStatus$Params, context?: HttpContext): Observable<StrictHttpResponse<Array<FavoriteStatusItemDto>>> {
  const rb = new RequestBuilder(rootUrl, favoritesControllerGetRestaurantFavoriteStatus.PATH, 'get');
  if (params) {
    rb.query('ids', params.ids, {});
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<Array<FavoriteStatusItemDto>>;
    })
  );
}

favoritesControllerGetRestaurantFavoriteStatus.PATH = '/api/v1/favorites/restaurants/status';
