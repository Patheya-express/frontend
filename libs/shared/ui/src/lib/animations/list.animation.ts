/** Class name for `keyframes.scss`'s list-item entrance animation, plus the directive that sets
 *  the `--stagger-index` custom property the keyframe's `animation-delay` reads. */
export const MOBILE_LIST_ITEM_TRANSITION = {
  enter: 'mobile-anim-list-item-enter',
} as const;
