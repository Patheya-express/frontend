apiVersion: v1
kind: Service
metadata:
  name: customer-app
  labels:
    app.kubernetes.io/name: customer-app
    app.kubernetes.io/part-of: patheya-express
    app.kubernetes.io/component: frontend
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: customer-app
  ports:
    - name: http
      port: 80
      targetPort: http
      protocol: TCP
